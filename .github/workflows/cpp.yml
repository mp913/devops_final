name: C/C++ CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - run: echo "Let's work" 
    - name: build and test
      run: docker build cpp-sample -t .
    - name: run
      run: docker run cpp-sample
      
